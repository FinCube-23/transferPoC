# Install QR Code Package

To enable the QR code feature, run the following command in the frontend directory:

```bash
npm install react-qr-code
```

or

```bash
yarn add react-qr-code
```

This will install the `react-qr-code` package which is used to generate QR codes for reference numbers in the Dashboard component.
